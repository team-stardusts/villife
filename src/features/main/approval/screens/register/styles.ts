import { StyleSheet } from "react-native";
import { UseNoticeRegisterScreenStylesType } from "./type";

export default function useNoticeRegisterScreenStyles(): UseNoticeRegisterScreenStylesType {
    return StyleSheet.create({
        contentsWrapper: {
            flex: 1,
        },
    });
}
