export type MessageSelectionModalProps = {
    vehicleID: number;
    //selectorType: SelectorType;
};

/* export const SELECTOR_TYPE = {
    CALL: "call",
    MESSAGE: "message",
} as const;

export type SelectorType = (typeof SELECTOR_TYPE)[keyof typeof SELECTOR_TYPE]; */
