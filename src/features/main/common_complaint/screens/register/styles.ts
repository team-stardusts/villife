import { StyleSheet } from "react-native";
import { UseCommonComplaintRegisterScreenStylesType } from "./type";

export default function useCommonComplaintRegisterScreenStyles(): UseCommonComplaintRegisterScreenStylesType {
    return StyleSheet.create({
        contentsWrapper: {
            flex: 1,
        },
    });
}
