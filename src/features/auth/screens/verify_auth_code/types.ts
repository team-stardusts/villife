import { NativeStackScreenProps } from "@react-navigation/native-stack";
import { VillifeStackParamList } from "../../../common/router/types";

type VerifyAuthCodeScreenProps = NativeStackScreenProps<VillifeStackParamList, "verify_auth_code">;

export default VerifyAuthCodeScreenProps;
