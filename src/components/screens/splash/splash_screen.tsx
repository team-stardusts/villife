import { SafeAreaView, Text, View } from 'react-native';

export default function SplashScreen() {

    return (
        <SafeAreaView>
            <View>
                <Text>Splash</Text>
            </View>
        </SafeAreaView>
    );
}